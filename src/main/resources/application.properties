spring.application.name=ForoHubAlura
 spring.datasource.url=jdbc:mysql://${DB_MYSQL_HOST}/${DB_MYSQL_NAME}
spring.datasource.username=${DB_MYSQL_USER}
spring.datasource.password=${DB_MYSQL_PASSWORD}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.security.secret = ${JWT-KEY}
